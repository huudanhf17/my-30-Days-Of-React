import React from "react";
import { withRouter } from "react-router";

function Profile(props) {
  return <div>If u see this, it means you are authenticated</div>;
}

export default withRouter(Profile);
