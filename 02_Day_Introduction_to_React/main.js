/* ------ Exercises: What is React? ------ */

// 1: What is React?
//React is a JavaScript library for building a reusable user interface(UI).

// 2: What is a library?
//Libraries are reusable code to perform specific actions. It is a collection of functions, objects, and classes we can use in our application.

// 3: What is a single page application?
//A single-page application is an app that works inside a browser and does not require page reloading during use.

// 4: What is a component ?
//Components are independent and reusable bits of code. They serve the same purpose as JavaScript functions, but work in isolation and return HTML via a render() function.
//Components come in two types, Class components and Function components

// 5: What is the latest version of React?
//v17.0.1

// 6: What is DOM?
//The Document Object Model (DOM) is a programming interface for HTML and XML documents. It represents the page so that programs can change the document structure, style, and content. The DOM represents the document as nodes and objects. That way, programming languages can connect to the page.

// 7: What is React Virtual DOM?
//The virtual DOM (VDOM) is a programming concept where an ideal, or “virtual”, representation of a UI is kept in memory and synced with the “real” DOM by a library such as ReactDOM.

/* ------ Exercises: Why is React? ------ */

// 1: Why did you chose to use react?
/*
fast
modular
scalable
flexible
big community and popular
open source
*/

// 2: What measures do you use to know popularity?
//It could be GitHub repository stars, watchers and forks.

// 3: What is more popular, React or Vue?
//React

/* ------ Exercises: JSX ------ */

// 1: What is an HTML element?
//An HTML element is defined by a start tag, some content, and an end tag.

// 2: How to write a self closing HTML element?
//<img src="" />

// 3: What is an HTML attribute? Write some of them
//HTML attributes provide additional information about HTML elements.
//src="*/*.jpg" alt=""

// 4: What is JSX?
//JSX is an XML/HTML-like syntax used by React that extends ECMAScript so that XML/HTML-like text can co-exist with JavaScript/React code.

// 5: What is babel?
//Babel is a toolchain that is mainly used to convert ECMAScript 2015+ code into a backwards compatible version of JavaScript in current and older browsers or environments.

// 6: What is a transpiler?
//Transpiler is a tool that takes source code like an inputs and outputs a new source code with the same task but has been modified accordingly.

/* ------ Exercises: JSX Elements ------ */

// 1: What is a JSX element?
//JSX element could be a single HTML element or many HTML elements wrapped in a parent HTML element.

// 2: Write your name in a JSX element and store it in a name variable
// const name = <h1>Ddddd</h1>

// 3: Write a JSX element which displays your full name, country, title, gender, email, phone number. Use h1 for the name and p for the rest of the information and store it in a user variable
/* const user = (
  <div>
  <h1>Ddddd</h1>
  <p>Vn, Male, thuudanh@xxx.xxx.vn, 0907788800</p>
  </div>
)
*/

// 4: Write a footer JSX element
/*
const footer = (
  <footer>
    <p>Copyright 2020</p>
  </footer>
)
*/
